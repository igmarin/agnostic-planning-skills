{
  "target_skill": "plan-tickets",
  "source_path": "skills/task-management/plan-tickets/SKILL.md",
  "instructions": [
    {
      "instruction": "Plan input -> classify each item -> draft tickets -> confirm before tracker creation",
      "original_snippets": "Plan input -> classify each item -> draft tickets -> confirm before tracker creation",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Do not create tracker issues unless the user explicitly asks for creation.",
      "original_snippets": "Do not create tracker issues unless the user explicitly asks for creation.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Do not assume tracker credentials, project fields, sprint IDs, status behavior, or required custom fields.",
      "original_snippets": "Do not assume tracker credentials, project fields, sprint IDs, status behavior, or required custom fields.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Assign these core planning attributes to each ticket:",
      "original_snippets": "Assign these core planning attributes to each ticket:",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create in issue tracker:**",
      "original_snippets": "**Create in issue tracker:**",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Verify the target project/board details first.",
      "original_snippets": "Verify the target project/board details first.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create issues **only after** the plan is considered approved enough.",
      "original_snippets": "Create issues **only after** the plan is considered approved enough.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Use whatever integration the user has (API, MCP, UI); do not assume credentials in the repo.",
      "original_snippets": "Use whatever integration the user has (API, MCP, UI); do not assume credentials in the repo.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Omit fields the project does not require. Confirm actual field names from the tracker's create-metadata endpoint before issuing the call. Do not set status on create — use the project's default initial status.",
      "original_snippets": "Omit fields the project does not require. Confirm actual field names from the tracker's create-metadata endpoint before issuing the call. Do not set status on create — use the project's default initial status.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "**[EXAMPLES.md](./EXAMPLES.md)** — Use when you need a full plan → ticket draft example with classification applied.",
      "original_snippets": "**[EXAMPLES.md](./EXAMPLES.md)** — Use when you need a full plan → ticket draft example with classification applied.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**[assets/ticket-samples/sample_issue.md](assets/ticket-samples/sample_issue.md)** — Use when you need the detailed format for a single issue.",
      "original_snippets": "**[assets/ticket-samples/sample_issue.md](assets/ticket-samples/sample_issue.md)** — Use when you need the detailed format for a single issue.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "When asked to draft tickets, your output MUST include:",
      "original_snippets": "When asked to draft tickets, your output MUST include:",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    }
  ]
}
