{
  "target_skill": "plan-tickets",
  "source_path": "skills/task-management/plan-tickets/SKILL.md",
  "instructions": [
    {
      "instruction": "Plan input -> classify each item -> draft tickets -> confirm before tracker creation",
      "original_snippets": "Plan input -> classify each item -> draft tickets -> confirm before tracker creation",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Do not create tracker issues unless the user explicitly asks for creation.",
      "original_snippets": "Do not create tracker issues unless the user explicitly asks for creation.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Do not assume tracker credentials, project fields, sprint IDs, status behavior, or required custom fields.",
      "original_snippets": "Do not assume tracker credentials, project fields, sprint IDs, status behavior, or required custom fields.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Do not re-plan if a plan already exists, unless there is a material gap.",
      "original_snippets": "Do not re-plan if a plan already exists, unless there is a material gap.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Do not add prefixes to tickets not owned by those areas unless requested.",
      "original_snippets": "Do not add prefixes to tickets not owned by those areas unless requested.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Draft each ticket using exactly five sections in this order:",
      "original_snippets": "Draft each ticket using exactly five sections in this order:",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Draft-only mode (Default):**",
      "original_snippets": "**Draft-only mode (Default):**",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Include a **Readiness Checklist** of what must be verified before creation: target project/board, tracker create-metadata endpoint, required fields, integration path, user approval, and default initial status.",
      "original_snippets": "Include a **Readiness Checklist** of what must be verified before creation: target project/board, tracker create-metadata endpoint, required fields, integration path, user approval, and default initial status.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "**Create-in-tracker mode:**",
      "original_snippets": "**Create-in-tracker mode:**",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create issues only after explicit user approval. Do not assume tracker credentials.",
      "original_snippets": "Create issues only after explicit user approval. Do not assume tracker credentials.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    },
    {
      "instruction": "Verify target project/board and confirm required fields from the tracker's create-metadata endpoint.",
      "original_snippets": "Verify target project/board and confirm required fields from the tracker's create-metadata endpoint.",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Omit fields not required. Do not set status on create (use project's default initial status).",
      "original_snippets": "Omit fields not required. Do not set status on create (use project's default initial status).",
      "relevant_when": "When a user asks for planning work that should trigger plan-tickets.",
      "why_given": "preference"
    }
  ]
}
